import { CreatePageHandle } from "./handles/CreatePageHandle"

const menus = [
    {
        // 创建查询页面
        command: "extension.hello.vue.element.demo",
        handle: CreatePageHandle
    }
]

export { menus }
